package fase3;

//This is the functional interface that must be implemented with the lamda expression
@FunctionalInterface
public interface ReversingInterface {
	//The only existing method accepts a string a return another string calling reverse()
	public String reverse(String reverse);

}
